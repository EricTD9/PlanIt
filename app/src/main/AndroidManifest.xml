<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Notificaciones: requerido desde Android 13 (API 33) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Alarmas exactas: requerido desde Android 12 (API 31) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    
    <!-- USE_EXACT_ALARM: solo para apps del sistema o con privilegios especiales -->
    <!-- Comentado porque no es necesario para apps normales -->
    <!-- <uses-permission android:name="android.permission.USE_EXACT_ALARM" /> -->
    
    <!-- Vibración -->
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- Alarmas: necesario para programar alarmas (Android 9+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Proyecto"
        android:name=".PlanItApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Proyecto">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <receiver
            android:name=".alarm.ReminderAlarmReceiver"
            android:exported="false" />
    </application>

</manifest>